    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Alces Storage Manager</a>
        </div>
    
          <ul class="nav navbar-nav navbar-right">
<% if session.has_key?(:authenticated_username) %>
            <li><p class="navbar-text">Logged in as <strong><%= session[:authenticated_username] %></strong></p></li>
            <li><%= link_to :controller => "login", :action => :logout do %>
              <span class="glyphicon glyphicon-log-out"></span> Log out
            <% end %>
            </li>
<% else %>
            <li><p class="navbar-text">Not logged in</strong></p></li>
<% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>